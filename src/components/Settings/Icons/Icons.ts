import { Book, DirectionFork, GameConsole, ListDropdown, MessageQueue } from "@vicons/carbon"
import { Book24Regular } from "@vicons/fluent"

export const FluentIcons = {
    "BibleBookIcon": Book,
    "SermonsIcon": MessageQueue,
    "DirectionIcon": DirectionFork,
    "PrayerListIcon": ListDropdown,
    "GameIcon": GameConsole
};